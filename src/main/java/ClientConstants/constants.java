package ClientConstants;

public class constants {
    public enum ds_type {
        string,
        hash,
        set,
        sorted_set,
        lists,
        streams,
        pub_sub,
        bitmap,
        bitfield,
        bloomfilter,
        hyperloglog

    }
}
